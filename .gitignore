# Rust build artifacts
/target

# Pipeline working data (mounted into the container as /work)
/work

# OS / editor cruft
.DS_Store
*.rs.bk

# OpenMVS writes <Tool>-<timestamp>.log files in its working directory
*.log
